server:
    port: 8888
    servlet:
    # 项目访问前缀
        context-path: /
        
#redis
redis:
  #redis机器ip
  host: 127.0.0.1
  #redis端口
  port: 6379
  #redis密码
  password: 123456
  #默认数据库
  database: 10
  #redis超时时间（毫秒），如果不设置，取默认值2000
  timeout: 10000
